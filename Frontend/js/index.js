import  data  from '../data/exampleData.json' with { type: 'json' };
import { CardContainer, menu, titulo, login } from "./components/index.js";


const app = document.getElementById('app');
app.className = 'w-screen h-screen p-4 text-gray-950 flex justify-center'

// const Cards = CardContainer(data.reportes);
// const menu_component = menu();
//const main = document.createElement('div');
//main.className = 'flex flex-row';
// main.appendChild(menu_component);
// main.appendChild(Cards);

// const titulo_component = titulo("Reportes");
// app.appendChild(titulo_component);
// app.appendChild(main);

//login

// const log = login();
// app.appendChild(log);

//registro


